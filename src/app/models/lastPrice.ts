export class LastPrice{
  ticker: string;
  timestamp: string;
  last: string;
  prevClose: string;
  open: string;
  high: string;
  low: string;
  mid: string;
  volume: string;
  bidSize: string;
  bidPrice: string;
  askSize: string;
  askPrice: string;

  // constructor(ticker: string, timestamp: string, last: string, prevClose: string,
  //             open: string, high: string, low: string, mid: string, volume: string,
  //             bidSize: string, bidPrice: string, askSize: string, askPrice: string) {
  //   this.ticker = ticker;
  //   this.timestamp = timestamp;
  //   this.last = last;
  //   this.prevClose = prevClose;
  //   this.open = open;
  //   this.high = high;
  //   this.low = low;
  //   this.mid = mid;
  //   this.volume = volume;
  //   this.bidSize = bidSize;
  //   this.bidPrice = bidPrice;
  //   this.askSize = askSize;
  //   this.askPrice = askPrice;
  // }
}
